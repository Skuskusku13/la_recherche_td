import {Text} from "@chakra-ui/react";

export const styles = {
    color: "green",
    textAlign: "center",
    fontSize: "1.5em",
}

export const SexionTitle = ({children}) => (
    <Text variant="maTeub">
        {children}
    </Text>
)